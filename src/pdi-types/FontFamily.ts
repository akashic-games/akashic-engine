/**
 * 文字列描画のフォントファミリ。
 * @deprecated 非推奨である。将来的に削除される。代わりに文字列 `"sans-serif"`, `"serif"`, `"monospace"` を利用すること。
 */
export enum FontFamily {
	/**
	 * サンセリフ体。ＭＳ Ｐゴシック等
	 */
	SansSerif,
	/**
	 * セリフ体。ＭＳ 明朝等
	 */
	Serif,
	/**
	 * 等幅。ＭＳ ゴシック等
	 */
	Monospace
}
